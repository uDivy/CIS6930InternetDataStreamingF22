Name: Divya Upadhyay
UFID: 2922-8848
Email: divyaupadhyay@ufl.edu

Environment Details:
Language: Python v3.9
IDE: PyCharm Community Edition 2022.1.1
OS: Windows 11 Home Single Language

The directory "Project2" contains the following files:
 1. Code for Count Min is available in the file "countmin.py".
 2. Code for Counter Sketch is available in the file "countersketch.py".
 3. Code for Active Counter is available in the file "activecounter.py".
 4. Output for Count Min is available in the file "countminoutput.txt".
 5. Output for Counter Sketch is available in the file "countersketchoutput.txt".
 6. Output for Active Counter is available in the file "activecounteroutput.txt".

Steps to run the code:
1. Open the python code in the PyCharm IDE.
2. Use the following steps to build and activate the venv(virtual environment): https://www.cybrosys.com/blog/how-to-setup-virtual-environment-in-pycharm
3. Once the virtual environment is created; you can activate the virtual environment by running the following cmd on the PyCharm terminal: 
	a. You should be at the project location where the 'venv' is created.
      b. Run the following in the terminal to activate the venv: <venv\Scripts\activate>
      c. You can confirm if the following 'venv' is activated if you are able to see the 'venv' is added in front of the project path in the terminal:
           (venv) D:\.....\Project2> 
4. Once the 'venv' is activated you can install the required python dependencies using the following command: 
    a. <pip install random>
    b. <pip install statistics>
5. Now, the environment setup is completed and you can run all the above codes one by one using: <python file_name.py> 
6. The code will automatically add the outputs in a dynamically generated file with the name mentioned above.
	